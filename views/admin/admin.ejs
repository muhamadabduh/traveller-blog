<!DOCTYPE html>
<html lang="en">

<head>

    <%-include('head')%>

</head>

<body>

    <div id="wrapper">

        <!-- Navigation -->
        <%-include('navbar')%>

        <div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">
                        <%=title%>
                    </h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- div class row -->
            <%if(content === 'create-post'){ %>
            <%-include('../post/create') %>
            <% } else if(content === 'register') { %>
            <%-include('../users/login') %>
            <% } else if(content === 'all-posts') { %>
            <%-include('../post/data-table')%>
            <% } else if(content === 'edit-post') { %>
            <% } else if(content === 'edit-post') { %>
            <% } else if(content === 'edit-post') { %>
            <% } else if(content === 'edit-post') { %>
            <% } %>
        </div>
        <!-- /#page-wrapper -->

    </div>
    <!-- /#wrapper -->

    <%-include('script')%>
</body>

</html>